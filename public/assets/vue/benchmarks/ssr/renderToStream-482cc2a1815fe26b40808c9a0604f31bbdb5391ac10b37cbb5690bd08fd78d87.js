"use strict";process.env.NODE_ENV="production";const Vue=require("../../dist/vue.runtime.common.js"),createRenderer=require("../../packages/vue-server-renderer").createRenderer,renderToStream=createRenderer().renderToStream,gridComponent=require("./common.js");console.log("--- renderToStream --- ");const self=global||root,s=self.performance.now(),stream=renderToStream(new Vue(gridComponent));let first,complete,str="";stream.once("data",()=>{first=self.performance.now()-s}),stream.on("data",e=>{str+=e}),stream.on("end",()=>{complete=self.performance.now()-s,console.log(`first chunk: ${first.toFixed(2)}ms`),console.log(`complete: ${complete.toFixed(2)}ms`),console.log()});